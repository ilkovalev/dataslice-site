# Находка: файл capstone.json (модуль 8) не подключён к сайту

**Severity:** важное (структурная, требует решения автора — НЕ правил автономно).

## Что
- Файл `src/content/lessons/capstone.json` существует: `id: capstone`, `module: 8`,
  title «Итог: ваш путь в данных», 5 битов, без виджета.
- Он **не импортирован** в `src/content/lessons/index.js` и **не входит** в массив `lessons`.
- Из-за этого `/stats/capstone` не находит урок и делает fallback на первый (center-measures).
- В модуле 8 на сайте живут 5 уроков: classification, confusion-matrix, roc, class-imbalance, overfitting.

## Почему не тронул автономно
Подключить или удалить урок — это авторское структурное решение (менять состав курса),
а не безопасная правка. Возможно, `capstone` (модуль 8) намеренно выведен и заменён на
`capstone-project` (модуль 12, «Капстоун: разбор реального датасета»). На сайте счётчик
«56 уроков» — совпадает с реестром без этого файла.

## Варианты (на выбор автора)
1. **Удалить** `capstone.json`, если он устарел/заменён на capstone-project — тогда файлов станет 56 = числу живых уроков.
2. **Подключить** в `index.js` (импорт + в массив модуля 8), если урок нужен — тогда я прогоню по нему ревью (simple/practice) как по остальным.

Пока файл не в курсе — ревью контента по нему не делалось.
